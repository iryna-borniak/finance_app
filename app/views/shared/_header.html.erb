<nav class="navbar 
            navbar-expand-lg 
            navbar-dark 
            bg-dark">
  <div class="container-fluid">
    <%= link_to root_path, 
                class: "navbar-brand 
                        text-white-50 
                        me-5" do %>
      <i class="bi bi-wallet-fill bi-custom"></i>
      BABULES
    <% end %>
    <button class="navbar-toggler" 
            type="button" 
            data-bs-toggle="collapse" 
            data-bs-target="#navbarNavDropdown" 
            aria-controls="navbarNavDropdown" 
            aria-expanded="false" 
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item me-4">
          <%= link_to "Головна", 
                      root_path, 
                      "aria-current": "page", 
                      class: "nav-link #{active_class(root_path)}" %>
        </li>
        <li class="nav-item me-4">
          <%= link_to "Транзакції", 
                      operations_path, 
                      "aria-current": "page", 
                      class: "nav-link #{active_class(operations_path)}" %>
        </li>
        <li class="nav-item me-4">
          <%= link_to "Категорії", 
                      categories_path, 
                      "aria-current": "page", 
                      class: "nav-link #{active_class(categories_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Звіти", 
                      reports_index_path, 
                      "aria-current": "page", 
                      class: "nav-link #{active_class(reports_index_path)}" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
