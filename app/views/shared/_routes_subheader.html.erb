<p style="color: green"><%= notice %></p>

<% if content_for?(:page_title) %>
  <div class="row mx-2 py-2">
    <div class="col p-0">      
      <%= yield(:page_title) %>      
    </div>
  </div>
<% end %>
